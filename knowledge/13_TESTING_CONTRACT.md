# Testing Contract

## Purpose
Testing requirements for all code.

## Scope
- Unit, Integration, E2E tests.

## Decisions
- Mocks are only allowed in tests.
- Backend uses `pytest`. Frontend uses `vitest`/`jest` and `playwright`.
- PRs require passing tests.

## Current Status
- Phase 0.
